<!DOCTYPE html>

<html>

<!-- Head -->
<head>

    <!-- Page title-->
    <title>PGA Tour Schedule</title>

    <!-- Meta tags -->
    <meta charset="UTF-8">

    <!-- CSS stylesheet -->
    <link rel="stylesheet" href="static/style.css">

    <!-- External scripts (JQuery, D3.js -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.0/jquery.min.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/d3js/7.8.4/d3.min.js"></script>

    <!-- Custom scripts -->
    <script src="static/script.js"></script>

</head>

<!-- Body -->
<body>

    <div id="content-wrapper">

        <!-- Wrapper for all elements of the chart, including svg, leaderboard, and any labels/captions -->
        <div id="chart-wrapper">

            <!-- Row at the top of the chart, displays tournament title -->
            <div id="chart-header">

                <div>
                    <h1 id="chart-title">The PGA Championship</h1>
                    <h2 id="chart-subtitle">Oak Hill Country Club - Rochester, NY</h2>
                </div>

                <div id="chart-options-bar">
                    <label id="chart-select-label" for="select-event-year">Select Tournament:</label>
                    <select name="event-year" class="chart-select" id="select-event-year">
                        <option value="">--Year--</option>
                    </select>
                    <select name="event-name" class="chart-select" id="select-event-name">
                        <option value="">--Event--</option>
                    </select>
                    <div id="chart-submit-button">Go</div>
                </div>

            </div>

            <!-- Horizontally arranged chart body elements - leaderboard and svg -->
            <div id="chart-body">

                <!-- Leaderboard appearing to the left of the svg -->
                <div id="leaderboard-table-wrapper">

                    <table id="leaderboard-table">

                        <thead>
                        <tr class="leaderboard-header-row">
                            <th class="leaderboard-col-rank">Rank</th>
                            <th class="leaderboard-col-name">Name</th>
                            <th class="leaderboard-col-score">Score</th>
                            <th class="leaderboard-col-round">R1</th>
                            <th class="leaderboard-col-round">R2</th>
                            <th class="leaderboard-col-round">R3</th>
                            <th class="leaderboard-col-round">R4</th>
                            <th class="leaderboard-col-total">Total</th>
                        </tr>
                        </thead>

                        <tbody id="leaderboard-table-body">

                        </tbody>

                    </table>

                </div>

                <!-- Placeholder for svg which is generated and appended here on page load -->
                <div id="chart-svg-wrapper"></div>

            </div>

        </div>

    </div>

</body>

</html>
